import 'package:xylophone/main.dart';
import 'package:flutter/material.dart';

void main() => runApp(ButtonPressed());

class ButtonPressed extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Tuhin"),
    );
  }
}
